#include "SolutionType.h"


// [SolutionType]
SolutionType::SolutionType()
{
  nq = 0.;
  ndim = 0.;
  rValue = NULL;
  state = NULL;
  transport = NULL;
}
// [SolutionType]


// [~SolutionType]
SolutionType::~SolutionType()
{
  if (rValue) delete [] rValue;
  rValue = NULL;
}
// [~SolutionType]


// [getRefValues]
double* SolutionType::getRefValues()
{
  return(rValue);
}
// [getRefValues]


// [getQx]
void SolutionType::getQx(const int& npts,
			 const double* x,
			 double* qx)
{
  cout << "\nQx values not defined for this SolutionType. "
       << "Returning null reference values." << endl;
  qx = NULL;
}
// [getQx]


// [getQy]
void SolutionType::getQy(const int& npts,
			 const double* x,
			 double* qy)
{
  cout << "\nQy values not defined for this SolutionType. "
       << "Returning null reference values." << endl;
  qy = NULL;
}
// [getQy]


// [getQz]
void SolutionType::getQz(const int& npts,
			 const double* x,
			 double* qz)
{
  cout << "\nQz values not defined for this SolutionType. "
       << "Returning null reference values." << endl;
  qz = NULL;
}
// [getQz]


// [getQxx]
void SolutionType::getQxx(const int& npts,
			  const double* x,
			  double* qxx)
{
  cout << "\nQxx values not defined for this SolutionType. "
       << "Returning null reference values." << endl;
  qxx = NULL;
}
// [getQxx]


// [getQyy]
void SolutionType::getQyy(const int& npts,
			  const double* x,
			  double* qyy)
{
  cout << "\nQyy values not defined for this SolutionType. "
       << "Returning null reference values." << endl;
  qyy = NULL;
}
// [getQyy]


// [getQzz]
void SolutionType::getQzz(const int& npts,
			  const double* x,
			  double* qzz)
{
  cout << "\nQzz values not defined for this SolutionType. "
       << "Returning null reference values." << endl;
  qzz = NULL;
}
// [getQzz]


// [getQxy]
void SolutionType::getQxy(const int& npts,
			  const double* x,
			  double* qxy)
{
  cout << "\nQxy values not defined for this SolutionType. "
       << "Returning null reference values." << endl;
  qxy = NULL;
}
// [getQxy]


// [getQxz]
void SolutionType::getQxz(const int& npts,
			  const double* x,
			  double* qxz)
{
  cout << "\nQxz values not defined for this SolutionType. "
       << "Returning null reference values." << endl;
  qxz = NULL;
}
// [getQxz]


// [getQyz]
void SolutionType::getQyz(const int& npts,
			  const double* x,
			  double* qyz)
{
  cout << "\nQyz values not defined for this SolutionType. "
       << "Returning null reference values." << endl;
  qyz = NULL;
}
// [getQyz]
