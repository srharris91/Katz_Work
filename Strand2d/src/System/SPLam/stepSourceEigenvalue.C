#include "StrandSPLam.h"


void StrandSPLam::stepSourceEigenvalue(const int& npts,
				       const double* q,
				       const double* qa,
				       double* sr)
{
  cout << "\nstepSourceEigenvalue in the StrandSPLam system layer not done"
       << endl;
  exit(0);
}
