#include "StrandSPTurbSA.h"


void StrandSPTurbSA::stepSourceEigenvalue(const int& npts,
					  const double* q,
					  const double* qa,
					  double* sr)
{
  cout << "\nstepSourceEigenvalue in the StrandSPTurbSA system layer not done"
       << endl;
  exit(0);
}
